<link href='http://fonts.googleapis.com/css?family=Lora:400,700' rel='stylesheet' type='text/css'>

<%= form_tag("/", method: :get) do  %>
 <%= text_field_tag :city %>
 <%= submit_tag "Go" %>
<% end %>

<container id="weather" data-city="<%= params[:city] %>">

	<% if @timenow.hour>17&&@timenow.hour<22 %>
	<h1>Good Evening</h1>
	<% elsif @timenow.hour>22&&@timenow.hour<6 %>
	<h1>Good Night</h1>
	<% elsif @timenow.hour>6&&@timenow.hour<22 %>
	<h1>Good Morning</h1>
	<% end %>
	<h1>let´s check out the weather!</h1>


	<svg 
     class="grid">
	</svg>

	<div class="icons"><%= image_tag("icon-wind.png", size: "100", class: "icon") %>
										 <%= image_tag("icon-temp.png", size: "100", class: "icon") %>
										 <%= image_tag("icon-clouds.png", size: "100", class: "icon") %>

	</div>

	<svg>
	</svg>
	<%= link_to "weatherdates", weatherdates_path %>
	<%= link_to "Collect my data!", new_kid_path %>

</container>


<button id="back" onclick="travelBack()" > Back </button>
<button id="ahead" onclick="travelAhead()" > Ahead </button>


